server:
  port: 10080

spring:
  profiles:
    active: local
  jpa:
    show-sql: true
  output:
    ansi:
      enabled: always

logging:
  level:
    com.kp.test: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type: TRACE